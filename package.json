{
  "name": "accessible-snapshot",
  "version": "0.1.0",
  "description": "A keyboard-first structural projection of any web page — browser extension for accessibility.",
  "private": true,
  "license": "MIT",
  "scripts": {
    "build": "node build.mjs",
    "watch": "node build.mjs --watch",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@types/chrome": "^0.1.36",
    "esbuild": "^0.24.0",
    "typescript": "^5.7.0"
  }
}
